dependencies:
  pre:
    #- wget -q -O - https://dl-ssl.google.com/linux/linux_signing_key.pub | sudo apt-key add - 
    #- sudo sh -c 'echo "deb http://dl.google.com/linux/chrome/deb/ stable main" >> /etc/apt/sources.list.d/google.list'
    #- sudo apt-get update
    #- sudo apt-get install google-chrome-stable
    - cd /tmp && wget -c https://dl.google.com/linux/direct/google-chrome-stable_current_i686.deb
    - sudo dpkg -i google-chrome-stable_current_i686.deb
    - sudo apt-get -f install

test:
  override:
    - touch $CIRCLE_ARTIFACTS/foo.txt
    - touch $CIRCLE_ARTIFACTS/screenshots/foo.png
    - mkdir $CIRCLE_ARTIFACTS/screenshots
    - touch $CIRCLE_ARTIFACTS/screenshots/bar.png

