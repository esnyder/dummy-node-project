machine:
  post:
    - mkdir daemondir
    - cd daemondir && date && echo `pwd` && sleep 10 && date && echo "done": {background: true}
    - date: {background: true}
    - echo "not running date"
        background: true

database:
  override:
    - echo "no database setup"
